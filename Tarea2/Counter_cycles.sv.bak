module Counter_cycles(
    input  cont, 
    output logic [7:0] total_cycles 
);

//Se define una variable intermedia 

logic [7:0] num_cycles = 0;


always @(posedge cont) begin

   num_cycles = num_cycles + cont;
	
	end
  
   assign total_cycles = num_cycles;  

endmodule

